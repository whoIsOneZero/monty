#include "monty.h"
/**
 * my_queue - prints the top of the queue
 * @head: ptr to the head of the stack
 * @counter: the line number
*/
void my_queue(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	context.lifi = 1;
}
